language: ruby
rvm:
 - 2.5.1
bundler_args: --jobs=2
before_install:
  - gem install coveralls
notifications:
    - false
scripts:
  - bundle exec rspec spec
after_script:
  - bundle exec rake coveralls:push  